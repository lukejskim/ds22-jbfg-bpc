maxim = 'Python Programming is Interesting!'

result = maxim.upper()      # 문자열을 모두 대문자로 변경
print(result)
result = maxim.lower()      # 문자열을 모두 소문자로 변경
print(result)
result = '/'.join(maxim)      # 문자열의 각 문자 사이에 '/'문자 집어 넣기
print(result)
